services:
  - type: web
    name: smart-employee-monitoring
    runtime: python
    buildCommand: pip install -r requirements-ai.txt
    startCommand: gunicorn --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT wsgi:app
    healthCheckPath: /health